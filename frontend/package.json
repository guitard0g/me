{
  "name": "frontend",
  "version": "0.1.0",
  "scripts": {
    "clean": "bsb -clean-world && rm -r src/*.bs.js && rm static/bundle.js",
    "build": "bsb -make-world && browserify src/*.bs.js -o static/bundle.js",
    "watch": "bsb -make-world -w"
  },
  "keywords": [
    "BuckleScript"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "bs-platform": "^5.0.6",
    "bucklescript-tea": "^0.11.2"
  },
  "dependencies": {
    "browserify": "^16.3.0"
  }
}
