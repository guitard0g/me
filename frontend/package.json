{
  "name": "frontend",
  "version": "0.1.0",
  "scripts": {
    "clean": "bsb -clean-world && rm -rf src/*.bs.js && rm static/bundle.js",
    "build": "bsb -make-world && browserify src/*.bs.js -o static/bundle.js",
    "watch": "bsb -make-world -w"
  },
  "keywords": [
    "BuckleScript"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "babel-preset-es2015": "^6.24.1",
    "bs-platform": "^5.0.6",
    "bucklescript-tea": "^0.11.2"
  },
  "dependencies": {
    "browserify": "^16.3.0"
  }
}
